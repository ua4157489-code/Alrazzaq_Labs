# ✅ Checklist — Lab 01: Installing Python & Environment Setup

## Task 1: Download and Install Python 3
- [x] Navigated to the official Python website (python.org).
- [x] Located the Downloads section.
- [x] Downloaded the correct Python 3.x installer for the OS in use.
- [x] Ran the installer (Windows: `Add Python 3.x to PATH` checked / macOS: `.pkg` installed / Linux: `apt-get install python3`).

## Task 2: Verify Installation
- [x] Opened a terminal / CLI.
- [x] Ran `python --version` (or `python3 --version`).
- [x] Confirmed the output shows a valid Python 3 version with no errors.
- [x] Confirmed `pip` is available (`python3 -m pip --version`).

## Task 3: Install an IDE or Text Editor
- [x] Chose an IDE/editor (VS Code and/or PyCharm Community).
- [x] Downloaded and installed it for the target OS.
- [x] Installed the Python extension (VS Code) or confirmed Python interpreter detection (PyCharm).
- [x] Verified the IDE's selected Python interpreter matches the version installed in Task 1.

## Overall Lab Outcome
- [x] Python 3 installed and confirmed working from the CLI.
- [x] Development environment configured and ready for coding.
- [x] No installation or PATH errors remaining.

**Lab Status: 100% Complete ✅**
